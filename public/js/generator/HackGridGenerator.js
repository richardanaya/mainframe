
var HackGridGenerator = function() {
	
}

var HackGridGenerator.generate = function( desiredSizeX, desiredSizeY, scene ) {
	var hackGrid = new HackGrid( desiredSizeX, desiredSize, scene );
	
	return hackGrid;
}